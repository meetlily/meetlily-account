export enum Path {
	Home = '/dashboard',
	Chat = '/chat',
	Settings = '/settings',
	NewChat = '/new-chat',
	Masks = '/masks',
	Auth = '/auth'
}
export enum SlotID {
	AppBody = 'app-body'
}
export enum CONFIG {
	NumList = 6
}
